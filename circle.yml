machine:
  services:
    - docker

dependencies:
  override:
    - sbt update

compile:
  override:
    - sbt docker:publishLocal

test:
  override:
    - sbt test

deployment:
  prod:
    branch: master
    commands:
      - . ./deploy.sh